// import { useBffStream } from "@bff-sdk/web";
import api from "api";

export default function () {
  // useBffStream(api.msg.msgInfo, {
  //   header: (data) => {
  //     console.log(data);
  //   },
  // });

  return <div>概览</div>;
}
